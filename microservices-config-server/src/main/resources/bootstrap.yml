spring:
  application:
    name: config-server
  
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jitendrar292/config-server-repo.git
    
security:
  user:
    password: password
    
server:
  port: 8888 